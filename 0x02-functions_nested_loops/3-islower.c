#include "main.h"
#include <ctype.h>
/**
 * _islower - Checks if charactor is lower uppercase letter
 * @c: char to check
 * Return: 1 for true / 0 for false
 */
int _islower(int c)
{
	return (islower(c));
}

#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 * @argc: numbers arguments passed
 * @argv: pointter to 1 daimentional array
 * Return: 0 success or 1 error
*/
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
